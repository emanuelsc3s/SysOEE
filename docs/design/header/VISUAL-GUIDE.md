# Guia Visual - AppHeader

## 🎨 Representação Visual do Componente

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           APFARMA HEADER (64px)                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌────────────────────────────────────────┐    ┌──────────────────────┐    │
│  │  APFARMA - Plataforma Integrada...    │    │  [●] Nome      [▼]  │    │
│  │  (20px, #242f65, Inter SemiBold)      │    │  32px  14px    16px  │    │
│  └────────────────────────────────────────┘    └──────────────────────┘    │
│   ← 24px padding                                                  24px →   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
  ↑                                                                           ↑
  Sticky top-0                                                         z-40
  
                                    ↓ (ao clicar)
                                    
                          ┌─────────────────────┐
                          │  Minha Conta        │
                          ├─────────────────────┤
                          │  👤 Perfil          │
                          ├─────────────────────┤
                          │  Sair               │
                          └─────────────────────┘
                          (z-50, align: end)
```

## 📐 Anatomia do Componente

```
Header Container
│
├─ Seção Esquerda (flex-start)
│  └─ Título
│     ├─ Texto: "APFARMA - Plataforma Integrada de Gestão"
│     ├─ Tamanho: 20px (text-xl)
│     ├─ Peso: 600 (font-semibold)
│     └─ Cor: #242f65 (brand-primary)
│
└─ Seção Direita (flex-end)
   └─ DropdownMenu
      ├─ Trigger (Button ghost)
      │  ├─ Avatar (32px circular)
      │  │  ├─ AvatarImage (se disponível)
      │  │  └─ AvatarFallback
      │  │     └─ User Icon (16px)
      │  │
      │  ├─ User Info (hidden < 768px)
      │  │  ├─ Nome (14px, medium)
      │  │  └─ Cargo (12px, regular)
      │  │
      │  └─ ChevronDown Icon (16px)
      │
      └─ Content (224px width)
         ├─ Label: "Minha Conta"
         ├─ Separator
         ├─ Item: Perfil
         ├─ Separator
         └─ Item: Sair
```

## 🎨 Paleta de Cores Visual

```
┌──────────────────────────────────────────────────────────────┐
│                    CORES DO APPHEADER                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ████████  #242f65  brand-primary     (Título, Avatar)      │
│  ████████  #ffffff  white             (Fundo do header)     │
│  ████████  #e5e7eb  border            (Borda inferior)      │
│  ████████  #6b7280  muted-foreground  (Texto "Admin")       │
│  ████████  #f3f4f6  accent            (Hover do botão)      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## 📏 Sistema de Espaçamento Visual

```
Header (64px altura)
│
├─ Padding Horizontal: 24px
│  │
│  ├─ [24px] Título [auto] [16px] UserMenu [24px]
│  │                         ↑
│  │                      gap-4
│  │
│  └─ UserMenu (Button)
│     │
│     ├─ Padding: 8px 16px
│     │
│     └─ Conteúdo (gap-2 = 8px)
│        │
│        ├─ Avatar (32px)
│        ├─ [8px]
│        ├─ UserInfo
│        ├─ [8px]
│        └─ ChevronDown (16px)
│
└─ Border Bottom: 1px
```

## 📱 Breakpoints Visuais

```
Mobile (< 768px)
┌─────────────────────────────────────┐
│ APFARMA    [●] [▼]                 │
└─────────────────────────────────────┘
  ↑            ↑   ↑
  Título    Avatar Dropdown
  (menor)   (visível)


Desktop (≥ 768px)
┌──────────────────────────────────────────────────────┐
│ APFARMA - Plataforma...    [●] Nome      [▼]        │
│                                 Admin                │
└──────────────────────────────────────────────────────┘
  ↑                            ↑   ↑        ↑
  Título completo           Avatar Nome   Dropdown
                                   (visível)
```

## 🎯 Estados Interativos

```
Normal
┌──────────────────┐
│ [●] Nome    [▼] │  ← Fundo transparente
└──────────────────┘

Hover
┌──────────────────┐
│ [●] Nome    [▼] │  ← Fundo cinza claro (#f3f4f6)
└──────────────────┘

Focus (Teclado)
┌──────────────────┐
│ [●] Nome    [▼] │  ← Anel azul 2px (#242f65)
└──────────────────┘
    ↑↑↑↑↑↑↑↑↑↑↑↑
    Ring offset 2px

Dropdown Aberto
┌──────────────────┐
│ [●] Nome    [▲] │  ← Chevron invertido
└──────────────────┘
         ↓
    ┌─────────────┐
    │ Minha Conta │
    ├─────────────┤
    │ 👤 Perfil   │
    ├─────────────┤
    │ Sair        │
    └─────────────┘
```

## 🔄 Fluxo de Interação

```
1. Página Carrega
   ↓
2. useAuth() → Busca usuário do Supabase Auth
   ↓
3. useQuery() → Busca dados da tabela tbusuario
   ↓
4. Renderiza Header
   ├─ Se loading: "Carregando..."
   ├─ Se erro: Fallback para email
   └─ Se sucesso: Nome do usuário
   ↓
5. Usuário Clica no Botão
   ↓
6. Dropdown Abre (Radix UI)
   ├─ aria-expanded="true"
   └─ data-state="open"
   ↓
7. Usuário Seleciona Opção
   ├─ Perfil → navigate('/perfil')
   └─ Sair → signOut() + navigate('/login')
```

## 🎨 Hierarquia Visual (Z-Index)

```
Camada 3 (z-50)
┌─────────────────┐
│ Dropdown Menu   │  ← Mais alto
└─────────────────┘

Camada 2 (z-40)
┌─────────────────────────────────────┐
│ Header Sticky                       │  ← Meio
└─────────────────────────────────────┘

Camada 1 (z-0)
┌─────────────────────────────────────┐
│ Conteúdo da Página                  │  ← Base
│                                     │
│                                     │
└─────────────────────────────────────┘
```

## 📊 Tipografia Visual

```
Título (h1)
APFARMA - Plataforma Integrada de Gestão
↑
20px (1.25rem) / 600 weight / #242f65

Nome do Usuário (p)
Emanuel Silva
↑
14px (0.875rem) / 500 weight / #141b1b

Cargo (p)
Administrador
↑
12px (0.75rem) / 400 weight / #6b7280
```

## 🎯 Avatar Visual

```
Com Foto                Sem Foto (Fallback)
┌──────────┐           ┌──────────┐
│          │           │    👤    │
│  [FOTO]  │           │          │
│          │           │  #242f65 │
└──────────┘           └──────────┘
  32×32px                32×32px
  rounded-full           bg-brand-primary
```

## 🔐 Fluxo de Autenticação

```
Login
  ↓
Supabase Auth
  ↓
JWT Token
  ↓
AppHeader
  ├─ useAuth() → user
  └─ useQuery() → tbusuario
      ↓
  Renderiza Nome + Avatar
      ↓
  Usuário Clica "Sair"
      ↓
  signOut()
      ↓
  Limpa Sessão
      ↓
  Redirect → /login
```

## 📱 Responsividade Detalhada

```
320px - 767px (Mobile)
┌─────────────────────┐
│ APFARMA  [●][▼]    │
└─────────────────────┘
  16px      32px 16px
  título    avatar

768px - 1023px (Tablet)
┌────────────────────────────────┐
│ APFARMA - Plat...  [●]Nome[▼] │
└────────────────────────────────┘
  20px                32px 14px

1024px+ (Desktop)
┌──────────────────────────────────────────────┐
│ APFARMA - Plataforma...  [●]Nome      [▼]   │
│                              Admin           │
└──────────────────────────────────────────────┘
  20px                       32px 14px/12px
```

---

**Legenda:**
- `[●]` = Avatar circular
- `[▼]` = ChevronDown icon
- `👤` = User icon
- `████` = Bloco de cor
- `↓` = Fluxo/sequência
- `├─` = Ramificação
- `└─` = Fim de ramificação

